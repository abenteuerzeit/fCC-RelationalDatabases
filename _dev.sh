#! /bin/bash
# Database helper strings

# PSQL="psql --username=freecodecamp --dbname=worldcup --no-align --tuples-only -c"
# LOGIN="psql --username=freecodecamp --dbname=postgres"
# SAVE_DB="pg_dump -cC --inserts -U freecodecamp worldcup > worldcup.sql"
# LOAD_DB="psql -U postgres < worldcup.sql"
# GRANT_EXE_TO_QUERIES="chmod +x ./queries.sh"
# GRANT_EXE_TO_INSERT="chmod +x ./insert_data.sh"
# CREATE_DB="CREATE DATABASE worldcup;"
# CONNECT="\c worldcup"